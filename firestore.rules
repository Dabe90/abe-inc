rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /inquiry_queue/{id} {
      allow read, write: if false;
    }
    match /inquiry_triage_log/{id} {
      allow read, write: if false;
    }
  }
}
